# AWARE API - Environment Configuration
# Copy this file to .env and fill in your values

# ============================================================================
# CLICKHOUSE DATABASE
# ============================================================================
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=8123
CLICKHOUSE_DATABASE=polybot

# ============================================================================
# API SERVER
# ============================================================================
API_HOST=0.0.0.0
API_PORT=8000
LOG_LEVEL=INFO

# Environment: development or production
# In production, CORS is restricted to ALLOWED_ORIGINS
ENV=development

# ============================================================================
# API AUTHENTICATION
# ============================================================================
# Comma-separated list of valid API keys
# Generate keys with: openssl rand -hex 32
API_KEYS=your-api-key-1,your-api-key-2

# Set to "false" to disable authentication (not recommended for production)
API_AUTH_ENABLED=true

# ============================================================================
# RATE LIMITING
# ============================================================================
# Rate limits are applied per-endpoint (see main.py)
# Default limits:
#   - /api/leaderboard: 100/minute
#   - /api/insider/alerts: 30/minute
#   - Other endpoints: no limit (add as needed)

# Future: Redis backend for distributed rate limiting
# REDIS_URL=redis://localhost:6379
